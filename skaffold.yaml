apiVersion: skaffold/v1beta2
kind: Config
build:
  artifacts:
  - image: changeme
    context: .
    docker: {}
  tagPolicy:
    envTemplate:
      template: '{{.DOCKER_REGISTRY}}/salaboy/snomed-test:{{.VERSION}}'
  local: {}
deploy:
  kubectl: {}
profiles:
- name: dev
  build:
    artifacts:
    - docker: {}
    tagPolicy:
      envTemplate:
        template: '{{.DOCKER_REGISTRY}}/salaboy/snomed-test:{{.DIGEST_HEX}}'
    local: {}
  deploy:
    helm:
      releases:
      - name: snomed-test
        chartPath: charts/snomed-test
        setValueTemplates:
          image.repository: '{{.DOCKER_REGISTRY}}/salaboy/snomed-test'
          image.tag: '{{.DIGEST_HEX}}'
